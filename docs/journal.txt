# Elder-Berry Journal
# Dieses Dokument ist die einzige laufende Quelle des Projektstands.
# Format: ## [Status]: [Phase] – [Beschreibung]

## Projekt initialisiert: 2026-03-13
- Repo erstellt
- Projektstruktur angelegt
- Phasen 1-5 definiert

## Abgeschlossen: Phase 1 – Teilschritt 1/5 – Projektstruktur + Systemdaten-Modul
- Branch: feature/phase-1-software-basic
- Dateien: src/elder_berry/system/info.py, tests/test_system_info.py, requirements.txt
- Tests: 4/4 grün, GPU-Test korrekt übersprungen (nvidia-smi nicht im Devcontainer)
- Offen: Ollama-Anbindung (heute Abend), PC-Steuerung, TTS (Windows)

## Abgeschlossen: Phase 1 – OOP-Refactoring: SystemMonitor-Klasse + LLMRouter DI
- system/info.py → SystemMonitor-Klasse (Funktionen → Methoden, DTOs bleiben)
- llm/router.py → Strict DI: __init__(ollama, openrouter) + LLMRouter.create_default() Factory
- Tests: 30/30 grün, 1 skip (GPU)

## Abgeschlossen: Phase 1 – Teilschritt 2/5 – LLM-Modul + Aktions-DB
- LLMRouter: Ollama (lokal) → OpenRouter (Fallback), transparenter Wechsel
- Dateien: src/elder_berry/llm/{base,ollama_client,openrouter_client,router}.py
- Dateien: src/elder_berry/actions/db.py (SQLite, CRUD, use_count self-learning)
- pyproject.toml angelegt (setuptools, pytest pythonpath konfiguriert)
- Tests: 26/26 grün, 1 skip (GPU), keine Warnings
- API-Key: OPENROUTER_API_KEY via GitHub Codespaces Secret / .env lokal
- Offen: Ollama live-Test (heute Abend Tower), PC-Steuerung, TTS (Windows)

## Abgeschlossen: Phase 1 – Bugfix: OllamaClient DEFAULT_MODEL
- ollama_client.py: DEFAULT_MODEL von "llama3.1:14b" auf "phi4:14b" korrigiert
- Ollama Live-Test erfolgreich: phi4:14b antwortet korrekt
- LLMRouter Live-Test erfolgreich: wählt Ollama als Backend
- Tests: 31/31 grün

## Abgeschlossen: Phase 1 – Teilschritt 3/5 – PC-Steuerung (ActionController)
- Branch: feature/phase-1-pc-steuerung
- Architektur: ABC (ActionController) + plattformspezifische Implementierung
- Dateien: src/elder_berry/actions/base.py (ABC + WindowInfo DTO)
- Dateien: src/elder_berry/actions/windows_controller.py (WindowsActionController)
- Dateien: tests/test_action_controller.py (21 Tests)
- Fähigkeiten: Tastatur (press/type/hotkey), Maus (move/click), Fenster (focus/min/max/list), Lautstärke (get/set/mute)
- Dependencies: pyautogui, PyGetWindow, pycaw, comtypes
- Interface-Abstraktion für spätere Linux-Unterstützung / Open Source
- Tests: 52/52 grün (21 neu + 31 bestehend)
- Offen: TTS (Windows), Integration LLM → ActionController

## Abgeschlossen: Phase 1 – Teilschritt 4/5 – Basis-TTS (Text to Speech)
- Branch: feature/phase-1-pc-steuerung
- Architektur: ABC (TTSEngine) + plattformspezifische Implementierung
- Dateien: src/elder_berry/tts/base.py (ABC + VoiceInfo DTO)
- Dateien: src/elder_berry/tts/windows_engine.py (WindowsTTSEngine via pyttsx3/SAPI5)
- Dateien: tests/test_tts.py (15 Tests)
- Fähigkeiten: speak, get/set rate, get/set volume, get/set voices
- Dependencies: pyttsx3
- Offline-fähig, kein API-Key nötig
- Tests: 67/67 grün (15 neu + 52 bestehend)
- Offen: Integration LLM → ActionController

## Abgeschlossen: Phase 1 – Teilschritt 5/5 – Integration (Assistant-Klasse)
- Branch: feature/phase-1-pc-steuerung
- Dateien: src/elder_berry/core/assistant.py (Assistant + AssistantResult DTO)
- Dateien: tests/test_assistant.py (23 Tests)
- Ablauf: User-Input → LLM (mit System-Prompt + DB-Aktionen) → JSON-Parsing → ActionController → TTS
- Alle Dependencies per DI: LLMClient, ActionsDB, ActionController, TTSEngine (optional)
- Robustes JSON-Parsing: direkt, Block-Extraktion, Fallback auf Rohtext
- Fehlerbehandlung: unbekannte Aktionen, fehlende Params, Controller-Fehler, TTS-Fehler
- Tests: 90/90 grün (23 neu + 67 bestehend)

## Phase 1 – Software Basic: ABGESCHLOSSEN
- Alle 5 Teilschritte erledigt
- Gesamtarchitektur: SystemMonitor, LLMRouter, ActionsDB, ActionController, TTSEngine, Assistant
- Konsistentes Pattern: ABC + plattformspezifische Implementierung + DI
- 90 Tests, alle grün

## In Arbeit: Phase 3 – Charakter / V-Tuber (vorgezogen, Phase 2 RPi5 verschoben)
- Branch: feature/phase-3-character-vtuber
- Teilschritte: 4 geplant
  1. CharacterEngine + Persönlichkeit Saleria Berry
  2. Coqui XTTS v2 – CoquiTTSEngine (Voice Cloning)
  3. Avatar-Display (PyGame Sprite Renderer)
  4. Integration in Assistant
- Voice Samples: 10 Arabella-WAVs → umbenannt zu Saleria (neutral, cheerful, sarcastic, motivated, thoughtful, whisper, shy, depressed, sad, angry)
- Referenz: Open-LLM-VTuber Repo analysiert – Emotions-Tagging durch LLM übernommen, Speaker-WAV pro Emotion ist unser Vorteil
- Nächster Schritt: TS 3 – Avatar-Display

## Abgeschlossen: Phase 3 – Teilschritt 1/4 – CharacterEngine + Saleria Berry
- Dateien: src/elder_berry/character/base.py (ABC + DTOs: Emotion, Personality, MoodState, EmotionMapping)
- Dateien: src/elder_berry/character/saleria.py (SaleriaEngine)
- Dateien: src/elder_berry/character/saleria.yaml (Persönlichkeit, Voice-Map, System-Prompt Template)
- Dateien: tests/test_character_engine.py (39 Tests)
- Emotions-Tagging: LLM setzt [emotion]-Tags → extract_emotion() parst → clean_response() entfernt
- Voice-Samples: 10 WAVs in src/elder_berry/tts/voices/ (pro Emotion ein Sample)
- Dependency: PyYAML>=6.0 in pyproject.toml hinzugefügt
- Tests: 129/129 grün (39 neu + 90 bestehend)

## Abgeschlossen: Phase 3 – Teilschritt 2/4 – CoquiTTSEngine (XTTS v2)
- Dateien: src/elder_berry/tts/coqui_engine.py (CoquiTTSEngine – XTTS v2 Voice Cloning)
- Dateien: src/elder_berry/tts/base.py (ABC erweitert: emotion-Parameter, load/unload, generate_audio)
- Dateien: src/elder_berry/tts/windows_engine.py (speak-Signatur angepasst, rückwärtskompatibel)
- Dateien: tests/test_coqui_tts.py (31 Tests)
- Features: Voice-Map (Emotion→Speaker-WAV), VRAM-Management (load/unload), Auto-Load, GPU/CPU-Fallback
- Audio-Playback: sounddevice + numpy (WAV lesen, normalisieren, abspielen)
- Dependencies: TTS>=0.22, sounddevice>=0.4, numpy>=1.26 als [tts-neural] optional
- Tests: 160/160 grün (31 neu + 129 bestehend)

## Abgeschlossen: Phase 3 – Teilschritt 3/4 – Avatar-Display (SpriteRenderer)
- Dateien: src/elder_berry/avatar/base.py (ABC AvatarRenderer)
- Dateien: src/elder_berry/avatar/sprite_renderer.py (SpriteRenderer – PyGame)
- Dateien: src/elder_berry/avatar/assets/ (10 Placeholder-Sprites, 512x512 PNG, transparent)
- Dateien: scripts/generate_placeholder_sprites.py (Sprite-Generator via Pillow)
- Dateien: tests/test_avatar_renderer.py (23 Tests)
- Features: Sprite pro Emotion, Sprech-Indikator, 30 FPS, Quit-Event
- Dependencies: pygame>=2.5 als [avatar] optional
- Tests: 183/183 grün (23 neu + 160 bestehend)

## Abgeschlossen: Phase 3 – Teilschritt 4/4 – Integration in Assistant
- Dateien: src/elder_berry/core/assistant.py (erweitert: CharacterEngine + AvatarRenderer per DI)
- Dateien: tests/test_assistant_integration.py (16 Tests)
- Flow: Input → LLM (Saleria-Prompt) → [emotion]-Tag extrahieren → Tag entfernen → Aktion → TTS (mit Emotion→Speaker-WAV) → Avatar (Emotion + Sprech-Indikator) → Result
- Rückwärtskompatibel: ohne CharacterEngine/Avatar verhält sich Assistant wie in Phase 1
- AssistantResult erweitert: emotion-Feld (str | None)
- Tests: 199/199 grün (16 neu + 183 bestehend)

## Phase 3 – Charakter / V-Tuber: ABGESCHLOSSEN
- Alle 4 Teilschritte erledigt
- Neue Klassen: CharacterEngine (ABC), SaleriaEngine, CoquiTTSEngine, AvatarRenderer (ABC), SpriteRenderer
- Charakter: Saleria Berry – "Charmant und melodisch mit spielerischer Gefahr"
- 10 Voice-Samples (Emotion→Speaker-WAV), 10 Placeholder-Sprites
- TTSEngine ABC erweitert (emotion, load/unload, generate_audio)
- Konsistentes Pattern: ABC + Implementierung + DI (wie Phase 1)
- 199 Tests, alle grün
- Dependencies: PyYAML (core), coqui-tts+sounddevice+numpy (optional [tts-neural]), pygame (optional [avatar])
- TTS→STT Roundtrip-Test: 96.3% Wort-Überlappung (neutral 89%, cheerful 100%, sarcastic 100%)
- pyproject.toml korrigiert: TTS>=0.22 → coqui-tts>=0.27 (original TTS inkompatibel mit Python 3.12)
- Live-Demo-Script: scripts/demo_tts_live.py (interaktiv Text eingeben, Emotion wählen, Saleria spricht)

## Phase 3 – Display-Entscheidung + Hardware (2026-03-14)
- Display-Typ: Pepper's Ghost (Acrylglas 45° + LCD)
- Avatar-Sprites: Layered Component System (Body/Eyes/Brows/Mouth separat, runtime compositing)
  - PSD in Arbeit, Export als 512×1024 PNGs mit transparentem Hintergrund
  - 5 Body-Poses, 3 Augenzustände (L+R), 3 Augenbrauen (L+R), 3 Mundzustände
  - LayeredSpriteRenderer noch zu implementieren (ersetzt aktuellen SpriteRenderer)

### Gekaufte Hardware:
| Teil                                    | Spezifikation                          | Preis   |
|-----------------------------------------|----------------------------------------|---------|
| Raspberry Pi 5 4GB                      | Einzelboard                            | ~86€    |
| RPi Touch Display 2 (5")               | 720×1280, DSI, kapazitiv, Portrait     | ~43,50€ |
| TECNOIOT 2S BMS (5 Stk.)              | 10A Dauer, 20A Limit, Tiefentladeschutz| ~15€    |
| Acrylplatten 150×200×1mm (15 Stk.)    | Transparent, für Pepper's Ghost        | ~9€     |
| microSD 32GB+                           | A2, Class 10 (noch zu kaufen)          | ~10€    |
| **Gesamt**                              |                                        |**~164€**|

### Display-Entscheidung Begründung:
- DSI-Anschluss: GPIO bleibt frei für Adafruit Motor HAT
- 720×1280 Portrait nativ: Perfekt für 512×1024 Sprite, kein Rotation-Hack
- Display-Strom: Separater 5V-Anschluss auf eigener PCB (vom D36V50F5)
- Pepper's Ghost: LCD horizontal, Acryl 45°, schwarzer Hintergrund → Sprite "schwebt"

### Energiebudget:
- 2× 18650 (35A E-Zig-Zellen, ohne Schutz) → 2S BMS → Sicherung 10A → PCB
- D36V50F5: 5V/5.5A für RPi5 + Display (~2-3A normal)
- Motoren: 4× TT-Motor direkt @ 7.4V (~0.8A normal, ~6A Stall)
- Normalbetrieb gesamt: ~3A, Worst Case ~8.5A → Sicherung 10A passt

### Gehäuse:
- Eigenbau: Resin-Druck (12kg Material vorhanden)
- Design in Phase 4 (Inventor), nach Hardware-Prototyp-Test

## Abgeschlossen: Phase 3 – LayeredSpriteRenderer
- Ersetzt SpriteRenderer: Component-basiertes Layering statt 1 PNG pro Emotion
- Assets exportiert und vereinheitlicht (snake_case):
  - body/: idle, angry, thinking (3 Poses)
  - eye/: open, close, angry, sad, surprise, side (6 Varianten × L+R, Brauen eingebrannt)
  - mouth/: neutral_close, idle_close, think_close, halfopen, open, angry_open (6 Varianten)
- Blink-Animation nur bei Standard-Augen (open↔close), nicht bei angry/sad/surprise/side
- Lip-Sync: Mund-Wechsel (close↔halfopen↔open) während show_speaking
- Dateien: src/elder_berry/avatar/layered_renderer.py (LayeredSpriteRenderer)
- Dateien: tests/test_layered_renderer.py (33 Tests)
- Pepper's Ghost optimiert: schwarzer Hintergrund (0,0,0)
- Tests: 232/232 grün (33 neu + 199 bestehend)

## In Arbeit: Phase 2 – RPi5 Kommunikation (Software-Seite, Hardware simuliert)
- Branch: feature/phase-2-rpi5-protocol
- Teilschritte:
  1. Protocol DTOs (gemeinsame Nachrichtentypen Tower ↔ RPi5)
  2. RobotServer (FastAPI, läuft auf RPi5 / Simulator)
  3. RobotClient (Tower-seitig, sendet Befehle)
  4. Simulator (Mock-RPi5 lokal auf Tower)
- Ordnerstruktur: chasis/electrik → hardware/electronics (+ enclosure, bom)
- Dependencies: fastapi, uvicorn[standard]

## Abgeschlossen: Phase 2 – TS 1-4 – Robot-Kommunikation (Simulator)
- Dateien: src/elder_berry/robot/protocol.py (DTOs: Commands, Status, Enums)
- Dateien: src/elder_berry/robot/server.py (FastAPI RobotServer + ABCs: MotorController, AvatarDisplay, SensorManager)
- Dateien: src/elder_berry/robot/client.py (RobotClient – httpx Tower→RPi5)
- Dateien: src/elder_berry/robot/simulator.py (SimulatedMotors/Avatar/Sensors)
- Dateien: tests/test_robot_protocol.py (35 Tests)
- Endpoints: /health, /status, /avatar/emotion, /motor/drive, /motor/stop, /sensor/battery, /sensor/all
- Simulator startet mit: python -m elder_berry.robot.simulator → localhost:8000
- Konsistentes Pattern: ABC + DI (MotorController, AvatarDisplay, SensorManager)
- Tests: 267/267 grün (35 neu + 232 bestehend)

## Abgeschlossen: Phase 2 – TS 5 – RobotClient → Assistant Integration
- Datei: src/elder_berry/core/assistant.py (erweitert: robot DI-Parameter)
- Datei: tests/test_assistant_robot.py (neu, 20 Tests)
- Datei: scripts/demo_integration.py (neu: Live-Test mit Simulator)
- Features:
  - Neue Aktionen: robot_drive, robot_stop
  - Emotion-Sync: Emotion automatisch an RPi5 weiterleiten
  - Speaking-Sync: Lip-Sync auf RPi5 parallel zum lokalen Avatar
  - Sensor-Status im System-Prompt (Akku-Warnung, Temperatur)
  - Graceful Degradation bei Robot-Offline (alle Robot-Calls abgesichert)
- Live-Test: 5/5 Szenarien bestanden (Simulator + Mock-LLM)
- Tests: 287/287 grün (20 neu + 267 bestehend)

## Phase 2 – RPi5 Integration (Software): ABGESCHLOSSEN
- Alle 5 Teilschritte erledigt (Protocol, Server, Client, Simulator, Assistant-Integration)
- Konsistentes Pattern: ABC + DI (wie Phase 1 und 3)
- 287 Tests, alle grün
- Hardware-Seite offen: echte RPi5-Klassen kommen wenn Hardware da ist

## Architektur-Entscheidung: 3-Tier-System (2026-03-14)
- Tower = Hirn (LLM + TTS-Generierung, immer an)
- Laptop = Client (PC-Steuerung + Audio-Empfänger, wenn User auf Couch)
- RPi5 = Robot-Körper (Motoren, Sensoren, Avatar-Display)
- TTS wird auf Tower generiert, WAV an aktiven Client gestreamt
- Laptop braucht AgentServer (FastAPI): /action/execute, /audio/play, /health
- AgentServer wrapped existierenden WindowsActionController
- RPi5 bekommt nur Lip-Sync-Signal, kein Audio (optional: Speaker für autonomen Betrieb)
- Nächster Schritt: AgentServer + AgentClient (elder_berry/agent/)

## Abgeschlossen: Phase 2 Erweiterung – AgentServer für Laptop-Steuerung
- Branch: feature/phase-2-rpi5-protocol
- Dateien: src/elder_berry/agent/protocol.py (DTOs: ActionRequest, ActionResult, AgentStatus, HealthResponse, ApiResponse)
- Dateien: src/elder_berry/agent/server.py (AgentServer – FastAPI, wraps ActionController, Audio-Playback multipart)
- Dateien: src/elder_berry/agent/client.py (AgentClient – httpx Tower→Laptop)
- Dateien: tests/test_agent_protocol.py (35 Tests)
- Dateien: tests/test_assistant_agent.py (14 Tests)
- Dateien: src/elder_berry/core/assistant.py (erweitert: agent DI-Parameter)
- Endpoints: /health, /status, /action/execute, /audio/play
- Features:
  - PC-Aktionen: Agent online → remote via AgentClient, offline → lokaler Fallback
  - Audio-Streaming: Tower generiert WAV → multipart Upload an Laptop → sounddevice Playback
  - TTS-Fallback: wenn generate_audio nicht verfügbar → lokales speak()
  - Agent-Fehler: automatischer Fallback auf lokale Ausführung
  - Robot-Aktionen gehen NICHT über Agent (direkt an RobotClient)
  - Rückwärtskompatibel: ohne Agent verhält sich Assistant wie bisher
- Dependencies: python-multipart>=0.0.18 (neu, für multipart Upload)
- pyproject.toml: [agent] optional dependency group hinzugefügt
- Eigene DTOs, unabhängig von robot/ (Entkopplung)
- Konsistentes Pattern: ABC + DI (wie alle anderen Module)
- Tests: 336/336 grün (49 neu + 287 bestehend)

## Planung abgeschlossen: Phase 6 – Matrix-Integration (2026-03-15)
- Konzeptdokument: docs/concepts/phase-6-matrix-integration.md (315 Zeilen)
- Server-Setup-Anleitung: docs/matrix_setup.md (~350 Zeilen)
- PROJECT_ROADMAP.md: Phase 6 ergänzt
- CLAUDE.md: Verweis auf docs/concepts/ ergänzt
- Domain: matrix.last-strawberry.com
- Server-Name: matrix.last-strawberry.com → User-IDs: @saleria:matrix.last-strawberry.com
- Synapse (Docker) + PostgreSQL 16 auf Plesk-Server
- Federation: AUS, E2EE: AUS (Phase 1)
- Teilschritte:
  1. TS1 – Synapse Server-Setup (manuell, Anleitung in docs/matrix_setup.md)
  2. TS2 – MessageChannel ABC + MatrixChannel (matrix-nio, async)
  3. TS3 – Audio-Pipeline (WAV → OGG/Opus für Element-Sprachnachrichten)
  4. TS4 – Assistant-Integration + Demo-Script
- Neue Klassen: MessageChannel (ABC), MatrixChannel, AudioConverter
- Neues Package: src/elder_berry/comms/
- Dependencies: matrix-nio, pydub (+ ffmpeg als System-Dependency)
- Branch: feature/phase-6-matrix-integration
- Nächster Schritt: Nutzer führt TS1 (Server-Setup) manuell aus, dann TS2 in Claude Code

## Gehäuse-Redesign: Ionische Säule → Baumstamm (2026-03-15)
- Alte Säule: 3kg bei 2mm Wand → zu schwer für TT-Motoren
- Neues Konzept: Holunder-Baumstamm, segmentiert (Kern + Rindenstücke)
- Spec: docs/concepts/gehaeuse-baumstamm-spec.md
- Bauraum Drucker: 223×126×230mm → Halbschalen + Einzelsegmente
- Zielgewicht Gehäuse: 500-600g (max. 750g)
- Gesamtgewicht Ziel: ~1.3kg (max. 1.5kg)
- Nächster Schritt: CAD in Inventor, Prototyp Kern-Halbschale drucken + wiegen

## Abgeschlossen: RPi5 Setup – Headless (2026-03-15)
- OS: Raspberry Pi OS Lite (64-bit, Bookworm)
- Python: 3.13.5 (neuer als Tower/Laptop 3.12 – OK, rückwärtskompatibel)
- Hostname: elderberry, IP: 192.168.50.220 (statisch)
- I²C aktiviert, Firewall aktiv (SSH + Port 8000), ffmpeg installiert
- venv: /home/pi/elder-berry/.venv
- Anleitung: docs/rpi5_setup.md
- CLAUDE.md aktualisiert (Python 3.13 statt 3.12)

## Abgeschlossen: Phase 6 – TS2 Vorbereitung (SecretStore + MessageChannel ABC + MatrixBridge)
- Branch: feature/phase-6-matrix-integration
- Dateien: src/elder_berry/core/secret_store.py (SecretStore – Fernet AES-128-CBC + HMAC-SHA256)
  - Key-Datei: ~/.elder-berry/secret.key (auto-generiert, chmod 600)
  - Secrets-Datei: ~/.elder-berry/secrets.enc (verschlüsseltes JSON)
  - API: get/set/delete/has/list_keys/get_or_none
  - Custom Exceptions: SecretStoreError, SecretNotFoundError
- Dateien: src/elder_berry/comms/__init__.py (Package-Init)
- Dateien: src/elder_berry/comms/message_channel.py (MessageChannel ABC + IncomingMessage DTO)
  - Interface: connect/disconnect/send_text/send_audio/on_message/sync_loop/is_connected
  - IncomingMessage: frozen Dataclass (sender, room_id, body, timestamp, raw)
  - MessageCallback Typ-Alias für async Callbacks
- Dateien: src/elder_berry/comms/bridge.py (MatrixBridge – Async↔Sync Bridge)
  - Thread + Event-Loop Pattern: async sync_loop in Hintergrund-Thread
  - run_in_executor für blockierendes Assistant.process()
  - Graceful Shutdown: disconnect + Task-Cancel
  - Fehlerbehandlung: Fehlermeldung an Raum bei Exception
- Dateien: tests/test_secret_store.py (28 Tests)
- Dateien: tests/test_comms.py (23 Tests)
- Dependencies: cryptography>=42.0 (core), matrix-nio>=0.24 + pydub>=0.25 (optional [matrix])
- pyproject.toml: [matrix] optional dependency group hinzugefügt
- Tests: 387/387 grün (51 neu + 336 bestehend)
- Nächster Schritt: MatrixChannel implementieren (wenn Synapse-Server steht)

## Abgeschlossen: Matrix-Server Setup – TS1 komplett (2026-03-15)
- Synapse (Docker) + PostgreSQL 16 auf Plesk-Server
- Domain: matrix.last-strawberry.com (SSL via Let's Encrypt)
- Plesk Docker Proxy Rules: matrix.last-strawberry.com → synapse:8008 (WebSocket aktiv)
- Accounts: @saleria:matrix.last-strawberry.com (Bot), @lera:matrix.last-strawberry.com (Admin)
- Federation: AUS, E2EE: AUS (encryption_enabled_by_default_for_room_type: "off"), Registration: AUS
- Raum erstellt (ohne E2EE, via curl – Element X erzwingt E2EE bei privaten Räumen)
- Saleria in Raum eingeladen
- Container-Pfad: /opt/matrix/ (docker-compose.yml + synapse-data/)

## Abgeschlossen: Phase 6 – TS2 MatrixChannel (matrix-nio Implementierung)
- Branch: feature/phase-6-matrix-channel
- Dateien: src/elder_berry/comms/matrix_channel.py (MatrixChannel – matrix-nio AsyncClient)
  - Login: Passwort oder Access-Token
  - Initialer Sync: markiert bestehende Nachrichten als gelesen
  - send_text: m.room.message (m.text)
  - send_audio: Upload + m.audio mit org.matrix.msc3245.voice Flag (Sprachnachricht)
  - on_message: Callback für m.room.message (m.text), eigene Nachrichten ignoriert
  - sync_loop: Polling mit Auto-Retry bei Netzwerkfehlern
  - Room-Whitelist: optionale Filterung auf erlaubte Räume
  - MatrixChannelError: eigene Exception-Klasse
- Dateien: tests/test_matrix_channel.py (35 Tests, Mock-basiert)
  - Init (5), Connect (4), Disconnect (2), Send Text (3), Send Audio (4)
  - On Message (9: eigene ignoriert, Whitelist, Callback-Fehler, Timestamp)
  - Sync-Loop (3: not connected, stop, retry)
  - MIME-Type (5)
- Dateien: scripts/demo_matrix_bot.py (Echo-Bot, nutzt SecretStore für Credentials)
- Dependencies: aiofiles>=23.0 zu [matrix] hinzugefügt
- Tests: 422/422 grün (35 neu + 387 bestehend)
- Nächster Schritt: Live-Test mit echtem Server (demo_matrix_bot.py)

## Abgeschlossen: Phase 6 – TS2 Live-Test + Auto-Join Fix
- Echo-Bot live getestet: demo_matrix_bot.py → "echo hallo" kam korrekt zurück
- Fix: _auto_join_invited_rooms() hinzugefügt – Bot tritt eingeladenen Räumen automatisch bei
- JoinError-Import ergänzt

## Abgeschlossen: Phase 6 – TS3 AudioConverter (WAV/MP3 → OGG/Opus)
- Dateien: src/elder_berry/comms/audio_converter.py (AudioConverter – pydub + ffmpeg)
  - ffmpeg-Verfügbarkeitsprüfung bei Init (shutil.which)
  - to_ogg_opus(): Konvertierung mit konfigurierbarer Bitrate, Returns (Path, duration_ms)
  - get_duration_ms(): Duration-Abfrage für Matrix-Event-Metadata
  - Lazy Import: pydub erst bei Methodenaufruf importiert
  - Graceful Degradation: klare Fehlermeldung wenn ffmpeg fehlt
- Dateien: tests/test_audio_converter.py (15 Tests)
  - Init/ffmpeg-Check (3), to_ogg_opus (7), ohne ffmpeg (2), get_duration (3)
  - Echte WAV-Fixtures (1s + 500ms Stille), Tests skippen wenn ffmpeg fehlt
- Dependencies: pydub>=0.25 bereits in [matrix] optional group
- Tests: 432/432 grün (15 passed + 0 skipped, 422 bestehend) – ffmpeg installiert via winget
- Nächster Schritt: TS4 – Assistant-Integration (MatrixBridge + LLM)

## Abgeschlossen: Phase 6 – TS4 Assistant-Integration (MatrixBridge + Audio-Pipeline)
- Datei: src/elder_berry/core/assistant.py (erweitert)
  - AssistantResult: neues Feld audio_path: Path | None
  - process(): neuer Parameter audio_output: Path | None (Datei statt Playback)
  - _tts_to_file(): generiert Audio als Datei (generate_audio), graceful fallback
- Datei: src/elder_berry/comms/bridge.py (erweitert)
  - AudioConverter als DI-Parameter (optional)
  - _handle_message(): WAV→OGG Pipeline (tempfile → convert → send_audio → cleanup)
  - _find_latest_audio() Placeholder entfernt
- Datei: tests/test_assistant_matrix.py (10 Tests, NEU)
  - audio_output: mit/ohne, Fehler, NotImplemented, leerer Input, kein TTS
  - AssistantResult.audio_path: default None, mit Pfad, rückwärtskompatibel
- Datei: tests/test_comms.py (angepasst: 3 alte Tests entfernt, 2 neue für AudioConverter-Integration)
- Datei: scripts/demo_matrix_bot.py (erweitert: --llm Flag für volle Pipeline)
  - Echo-Modus (Standard): wie bisher
  - LLM-Modus: Ollama + TTS + AudioConverter + MatrixBridge
- Tests: 451/451 grün (19 neu/geändert + 432 bestehend)
- Rückwärtskompatibel: ohne audio_output verhält sich process() wie bisher

## Architektur-Notiz: Remote Tower via NordVPN Meshnet (2026-03-15)
- Laptop unterwegs kann Tower als LLM-Backend nutzen über NordVPN Meshnet
- Meshnet gibt feste Mesh-IPs (100.x.x.x), kein Port-Forwarding nötig
- Voraussetzung Tower: OLLAMA_HOST=0.0.0.0 + Firewall nur Mesh-IP auf Port 11434
- LLMRouter Config: ollama.host = Mesh-IP (unterwegs) oder localhost (zuhause)
- Idee: Auto-Erkennung in LLMRouter (localhost erreichbar? → lokal, sonst → Mesh-IP)
- Latenz unterwegs: 30-80ms (gutes Netz), 150-500ms (schlechtes Netz)
- Streaming-Tokens über Mesh merkbar langsamer, aber funktional
- Fallback: Laptop hat eigenes Ollama (phi4:14b mit RAM-Auslagerung)
- Aktueller Entwicklungsbetrieb: alles auf Laptop, Tower wird später scharfgeschaltet

## Phase 6 – Matrix-Integration: ABGESCHLOSSEN
- Alle 4 Teilschritte erledigt (Server-Setup, MatrixChannel, AudioConverter, Assistant-Integration)
- Neue Klassen: SecretStore, MessageChannel (ABC), MatrixChannel, MatrixBridge, AudioConverter
- Live-getestet: Text + Sprachnachricht via Element funktional
- 451 Tests, alle grün
- Konsistentes Pattern: ABC + DI (wie alle anderen Module)
- Demo: scripts/demo_matrix_bot.py (Echo-Modus + --llm für volle Pipeline)

## Stand nach Phase 6 – Projektübersicht (2026-03-15)
- Phase 1 (Software Basic): ABGESCHLOSSEN
- Phase 2 (RPi5 Anbindung): SOFTWARE ABGESCHLOSSEN, Hardware offen
- Phase 3 (Charakter/V-Tuber): ABGESCHLOSSEN
- Phase 4 (Körper): IN ARBEIT (CAD/Gehäuse), Software offen
- Phase 5 (Software Advance): OFFEN
- Phase 6 (Matrix-Integration): ABGESCHLOSSEN
- Gesamtarchitektur: 15 Kernklassen, konsistentes ABC+DI Pattern
- Tests: 451 grün

## Abgeschlossen: Quick-Win – SystemMonitor in Assistant integrieren
- system_status Aktion: LLM kann PC-Zustand abfragen (CPU, RAM, GPU, Top-Prozesse)
- SystemMonitor als optionaler DI-Parameter im Assistant
- Formatierter Output: CPU%, RAM MB/%, GPU Name+Auslastung+VRAM+Temp, Top-5 Prozesse
- Graceful Degradation: ohne Monitor → action_success=False, kein Crash
- Datei: src/elder_berry/core/assistant.py (erweitert: system_monitor DI + _get_system_status)
- Datei: tests/test_assistant_system.py (8 Tests, NEU)
- Tests: 459/459 grün (8 neu + 451 bestehend)

## Planung: Saleria Remote-Features + Claude-Agent-Mode (2026-03-15)
- Konzept: docs/concepts/saleria-remote-features.md
- Direkte Features (kein LLM): Systemstatus, Screenshot, Medien, Prozesse, Datei-Zugriff, WoL, Clipboard
- Claude-Agent-Mode: Saleria nutzt Anthropic API (Sonnet) für komplexe Anfragen
  - Structured JSON Output, Whitelist-Aktionen, Kontext aus CLAUDE.md + journal.txt
  - Darf: docs/ lesen/schreiben, journal.txt appenden, Tests lesen, Status abfragen
  - Darf NICHT: src/ ändern, Shell ausführen, Pakete installieren (→ das macht Claude Code)
- Proaktive Alerts: Disk voll, Download fertig, Prozess crashed
- Neue Klassen: ClaudeAgent, RemoteCommandHandler, SystemMonitor
- Abgrenzung: Saleria = Augen/Ohren (Remote), Claude Code = Hirn (Entwicklung)

## Hardware-Entscheidung: Motor-Upgrade TT → JGB37-520 (2026-03-15)
- Grund: Gehäuse 1.138g + Elektronik ~760g + Chassis ~400g = ~2.3kg → TT-Motoren zu schwach
- Neuer Motor: JGB37-520 (12V, Hall-Encoder, 2-10 kg·cm je nach Ratio)
- Traglast mit 4× JGB37: 5-8kg → massig Reserve für ~3kg Roboter
- Encoder ermöglicht PID-Regelung auf Pico 2W
- Konsequenzen:
  - Neues Chassis nötig (JGB37 = Ø37mm Getriebe, deutlich größer als TT)
  - 4× Motor = 784g (statt 120g bei TT) → Gesamtgewicht steigt auf ~2.7-3kg
  - Motor-Driver: Adafruit Motor HAT evtl. zu schwach (1.2A/Kanal, JGB37 Stall = 2.8A)
    → Alternative evaluieren: L298N, BTS7960, oder Custom-Driver auf eigener PCB
  - Akku: 2S (7.4V) läuft, aber JGB37 ist für 12V spezifiziert (60% Drehzahl bei 7.4V)
    → 3S (11.1V) prüfen: BMS-Kompatibilität, D36V50F5 Eingangsbereich, PCB-Redesign
  - Mecanum-Räder: 60mm oder 80mm mit 6mm D-Shaft Aufnahme
- OFFEN: Akku 2S vs 3S → BMS und PCB prüfen
- OFFEN: Motor-Driver Wahl → Phase 4 PCB-Redesign
- OFFEN: Chassis-Design → nach Motor-Bestellung

## Abgeschlossen: Phase 7 – Schritt 1+2 – RemoteCommandHandler + Command-Router
- Branch: feature/phase-7-remote-features
- Dateien: src/elder_berry/comms/remote_commands.py (RemoteCommandHandler + CommandResult DTO)
  - Tier-1-Commands: status, systemstatus, screenshot, screen, pause, play, skip, next, prev, previous
  - Volume-Command: volume/vol/lautstärke <0-100>
  - DI: SystemMonitor (optional), ActionController (optional)
  - Graceful Degradation: fehlende Dependencies → Fehlertext, kein Crash
  - Screenshot: mss → PNG temp-Datei, cross-platform
  - Media: press_key(playpause/nexttrack/prevtrack) via ActionController
- Dateien: src/elder_berry/comms/message_channel.py (erweitert: send_image() mit Default-NotImplementedError)
- Dateien: src/elder_berry/comms/matrix_channel.py (erweitert: send_image() für m.image Upload + _guess_image_mime_type)
- Dateien: src/elder_berry/comms/bridge.py (erweitert: Command-Router)
  - RemoteCommandHandler als optionaler DI-Parameter
  - _handle_message() → parse_command() → Command erkannt? → execute → send result
  - Kein Command → weiter an Assistant (bestehender Flow, refactored in _handle_assistant_message)
  - Screenshot: send_image() mit NotImplementedError-Fallback
- Dateien: tests/test_remote_commands.py (37 Tests, NEU)
- Dateien: tests/test_matrix_channel.py (erweitert: 11 neue Tests – send_image + Image-MIME-Type)
- Dateien: tests/test_comms.py (erweitert: 6 neue Tests – Bridge Command-Routing)
- Dependencies: mss>=9.0 in [remote] optional group (pyproject.toml)
- SystemMonitor: bestehende Klasse wiederverwendet (kein neues File)
- Tests: 513/513 grün (54 neu + 459 bestehend)
- Konsistentes Pattern: DI + Graceful Degradation (wie alle Module)

## Abgeschlossen: Phase 7 – Nachbesserungen (gleicher Chat)
- Keyword-basiertes Command-Parsing: natürliche Sprache erkannt ("schick mir ein screenshot")
- demo_matrix_bot.py: --remote Flag (standalone oder mit --llm kombinierbar)
- Saleria Persönlichkeit: von "magische Hexe" zu locker-frech (Option A)
- Datum/Uhrzeit im System-Prompt (LLM kennt jetzt den Wochentag)
- Aktions-Anweisung: "nur wenn explizit gefragt" (verhindert unnötige Fenster-Fokus-Aktionen)
- Ollama-Timeout: 60s → 120s (konfigurierbar, für Laptop mit RAM-Auslagerung)
- Tests: 518/518 grün
- Nächster Schritt: ClaudeAgent (Stufe 2 – Anthropic API für komplexe Anfragen)

## Abgeschlossen: Phase 7 – Schritt 3 – ClaudeAgent (Claude API für komplexe Anfragen)
- Branch: feature/phase-7-claude-agent
- Dateien: src/elder_berry/comms/claude_agent.py (ClaudeAgent + AgentResult DTO)
  - Anthropic API Client (claude-sonnet-4-6, latest)
  - Kontext: CLAUDE.md + journal.txt (letzte 80 Zeilen) automatisch geladen
  - Structured JSON Output mit robustem Parsing (direkt, Codeblock, Brace-Extraktion)
  - Whitelist-Validierung: 10 erlaubte Aktionen
  - Pfad-Sicherheit: Path-Traversal-Schutz, write nur in docs/, append nur journal.txt
  - Aktionen: read_file, write_file, append_file, list_directory, search_files,
    system_status, screenshot, run_tests, git_status, answer_only
  - Graceful Degradation: API-Fehler, ungültiges JSON → Fallback auf answer_only
- Dateien: src/elder_berry/comms/bridge.py (erweitert: ClaudeAgent DI + Keyword-Routing)
  - Neuer DI-Parameter: claude_agent (optional)
  - Keyword-Routing: "claude" im Text + Auftrag in Anführungszeichen → ClaudeAgent
  - extract_claude_message(): extrahiert Text aus "..." wenn "claude" vorkommt
  - Ohne Keyword → lokales LLM (Saleria-Persönlichkeit + TTS)
  - Command-Router-Kette: Command → RemoteCommandHandler → claude+"..." → ClaudeAgent → LLM
  - _handle_claude_agent(): Screenshot-Bild senden, Details kürzen, Fehlerbehandlung
- Dateien: scripts/demo_matrix_bot.py (erweitert: --agent Flag)
  - create_claude_agent(): API-Key aus SecretStore, Projekt-Root auto-detect
  - Kombinierbar: --llm --remote --agent (alle drei gleichzeitig)
  - Anleitung im Log: 'claude "Dein Auftrag hier"'
- Dateien: tests/test_claude_agent.py (62 Tests, NEU)
  - AgentResult DTO (3), Init (3), Kontext-Laden (5), JSON-Parsing (5),
    Aktions-Validierung (5), Pfad-Validierung (7), read_file (6), write_file (4),
    append_file (3), list_directory (4), search_files (3), answer_only (1),
    git_status (1), process E2E (11), Import-Fehler (1)
- Dateien: tests/test_comms.py (erweitert: 9 extract_claude_message + 8 Bridge-Routing)
- Dependencies: anthropic>=0.40 in [remote] optional group (pyproject.toml)
- API-Key: SecretStore().set("anthropic_api_key", "sk-ant-...")
- Tests: 510 passed, 8 vorbestehende Failures (pydub fehlt)
- Konsistentes Pattern: DI + Graceful Degradation (wie alle Module)

## Architektur-Entscheidung: Stationär + Drehbar statt Mobil (2026-03-15)
- Mecanum-Antrieb gestrichen – Mehrwert zu gering für den Aufwand
- Pepper's Ghost Blickwinkel funktioniert nur von vorne/oben → auf Boden unsichtbar
- Neues Konzept: Drehteller (Servo + Kugellager) unter dem Baumstamm
- Saleria dreht sich zum User → visueller Effekt ausreichend
- Was wegfällt:
  - 4× JGB37-520 Motoren (784g, ~30€)
  - Mecanum-Chassis komplett
  - Motor-Driver (L298N/BTS7960)
  - Pico 2W als Motor-Controller (bleibt evtl. für Sensoren)
  - PID-Regelung, Pfadplanung, Kollisionserkennung
  - Absturzsicherung (Tischkante)
  - Akku-Dimensionierung für Motoren → Akku wird deutlich kleiner/leichter
  - Phase 4 Chassis-Design komplett
- Was neu kommt:
  - 1× Servo (SG90 oder MG996R, ~3-5€)
  - Drehteller/Kugellager (Lazy Susan, ~5€)
  - Einfache Servo-Steuerung über RPi5 GPIO
- Gehäuse-Gewicht (1.138g) ist kein Problem mehr – Tisch trägt das Gewicht
- Standort: noch unklar (Schreibtisch, Regal, Sideboard)
- Stromversorgung: USB-C vom Netzteil statt Akku möglich → unbegrenzte Laufzeit

## Hinweis: Robot-Code nach Mobilitäts-Streichung (2026-03-15)
- RobotServer/Client (src/elder_berry/robot/) existiert noch
- Motor-Endpoints (drive, stop) werden vorerst nicht gelöscht aber sind Dead-Code
- Avatar-Display + Sensor-Endpoints bleiben relevant
- Drehteller-Servo könnte als neuer Endpoint in RobotServer ergänzt werden
  - z.B. POST /servo/rotate {angle: 90} oder POST /servo/face_user
- Bestehende Tests (robot_protocol) bleiben grün, nichts brechen

## Abgeschlossen: Phase 7 – Tier 2 + Tier 3 + Proaktive Alerts
- Branch: feature/phase-7-tier2-tier3-alerts
- Fortsetzung von Phase 7 (Schritt 1-3 fertig: RemoteCommandHandler, Command-Router, ClaudeAgent)
- Datei: src/elder_berry/comms/remote_commands.py (erweitert: 8 neue Commands)
  - Tier 2: clipboard (lesen), clip_write (schreiben), send_file (Pfad→Matrix),
    start_process (Whitelist), kill_process (Whitelist), wol (Magic Packet)
  - Tier 3: git (status/pull/log/diff), docker (ps/restart/logs), download (httpx)
  - Sicherheit: Start/Kill-Whitelists, Größenlimit (50MB), Pfad-Validierung, URL-Validierung
- Datei: src/elder_berry/comms/message_channel.py (erweitert: send_file() ABC-Methode)
- Datei: src/elder_berry/comms/matrix_channel.py (erweitert: send_file() + _guess_file_mime_type)
- Datei: src/elder_berry/comms/bridge.py (erweitert: send_file Routing + AlertMonitor DI)
- Datei: src/elder_berry/comms/alert_monitor.py (NEU: AlertMonitor + AlertConfig)
  - Daemon-Thread, konfigurierbares Poll-Intervall
  - Disk-Check: Warnung bei >90% Belegung (Schwellwert konfigurierbar)
  - Prozess-Check: Warnung wenn überwachter Prozess crashed
  - Alert-Deduplizierung: gleicher Alert wird nicht wiederholt
  - Thread-safe Alert-Callback via asyncio.run_coroutine_threadsafe
- Dependencies: pyperclip>=1.9 in [remote] optional group (pyproject.toml)
- Tests: tests/test_remote_commands.py (97 Tests, 60 neu + 37 bestehend)
- Tests: tests/test_comms.py (erweitert: 5 neue Tests – send_file Routing + AlertMonitor)
- Tests: tests/test_alert_monitor.py (NEU, 14 Tests)
- Tests: 686/686 grün (176 neu + 510 bestehend)
- Konsistentes Pattern: DI + Graceful Degradation + Whitelists (wie alle Module)

## In Arbeit: Gehäuse – RootPaths.iLogicVb (Wurzeln am Stammfuß)
- Datei: hardware/enclosure/iLogic/RootPaths.iLogicVb (NEU)
- 3D-Spline-Pfade für Wurzeln + Sweep-Profile (konisch) + Fillet-Versuch
- Parametrisch: 6 Wurzeln, Länge 4cm, Dicke 1.8→0.4cm, Seed 99
- Stil/API konsistent mit BarkPaths.iLogicVb

## Abgeschlossen: Gehäuse – RootPaths.iLogicVb (Wurzeln am Stammfuß)
- Datei: hardware/enclosure/iLogic/RootPaths.iLogicVb (260 Zeilen, NEU)
- 6 Wurzeln als 3D-Spline-Pfade: radial nach außen (4cm) + unter Z=0 (-1.5cm)
- Z-Kurve: startet bei Z=1.5cm am Stamm → Bodenniveau → -1.5cm (unter Boden)
- Konisches Sweep-Profil: Ø1.8cm (Basis) → Ø0.4cm (Spitze) mit Taper-Winkel
- Fallback-Kette: Taper-Sweep → einfacher Sweep → nur Spline-Pfad (manuell)
- Fillet-Versuch am Wurzel-Fuß (Edge-Finding per Distanz, Try/Catch)
- Cleanup: Re-Run löscht alle Root_-prefixed Elemente (Features → Sketches → WP)
- Parametrisch: Anzahl, Länge, Dicke, Absenkung, Fillet, Seed (99 ≠ BarkPaths 42)
- Stil konsistent mit BarkPaths.iLogicVb (gleiche API-Patterns)
- Risiken: Taper-API und Fillet-Edge-Finding können je nach Inventor-Version/Geometrie fehlschlagen

## Update: RootPaths.iLogicVb – Parameter-Fix nach erstem Test
- Self-Intersection bei 3/6 Wurzeln → Welligkeit 0.3→0.12, Frequenz 1.5→1.0, Punkte 10→8
- Wurzeln zu klein → Länge 4→6cm, Dicke 1.8→2.5cm, Absenkung 1.5→2.5cm, Anstieg 1.5→2.5cm
- Fillet 0.5→0.8cm, Spitze 0.4→0.6cm

## Update: RootPaths.iLogicVb v2 – Komplett-Rewrite (organischer)
- Zwei Klassen: 4 Hauptwurzeln (Ø2.8cm, 6.5cm lang) + 5 Nebenwurzeln (Ø1.0cm, 4cm lang)
- Variable Exponenten pro Wurzel: radialExp, anstiegExp, absenkExp → jede Wurzel anders
- Flacher: Anstieg 1.2cm (Haupt) / 0.6cm (Neben) statt 2.5cm → horizontal statt Kralle
- Absenkung spät (Exp 1.5–3.0) → Wurzel läuft erst flach, taucht dann sanft ab
- Winkelvariation 0.7 → unregelmäßige Verteilung
- Welligkeit 0.10cm, Frequenz 0.8–1.2 → kaum seitliche Kurven
- Fillet nur bei Hauptwurzeln (Nebenwurzeln zu dünn)
- Nebenwurzeln versetzt zwischen Hauptwurzeln platziert
- Feature-Namen: Root_Main_XX / Root_Side_XX (unterscheidbar im Baum)

## Abgeschlossen: Phase 7 – Avatar-Selfie Command
- Branch: feature/phase-7-tier2-tier3-alerts
- Neuer Command: "avatar" / "selfie" → Saleria rendert Avatar-Bild → Matrix
- Optional mit Emotion: "selfie angry", "avatar cheerful" (10 Emotionen)
- Keywords: "zeig dich", "wie siehst du aus", "bild von dir"
- Datei: src/elder_berry/avatar/base.py (erweitert: render_to_file() ABC)
- Datei: src/elder_berry/avatar/layered_renderer.py (erweitert: render_to_file + headless-Modus)
  - _ensure_components_loaded(): lädt Sprites ohne Display-Fenster
  - _blit_to(): generische Blit-Methode auf beliebige Surface
- Datei: src/elder_berry/comms/remote_commands.py (erweitert: avatar DI + Command)
- Tests: 697/697 grün (11 neu + 686 bestehend)

## Update: RootPaths.iLogicVb v3 – Komplett-Rewrite (L-Kurve + Ellipse)
- L-förmige Z-Kurve (stückweise): Drop→Flat→Dive statt Polynom-Sichel
  - Phase 1 (t<0.08-0.20): Schnell auf Bodenniveau
  - Phase 2 (Hauptteil): Lang flach am Boden (Z≈0, leichte Wellung)
  - Phase 3 (t>0.70-0.92): Sanft in den Boden
  - Phasengrenzen pro Wurzel variabel → jede Kurve anders
- Elliptisches Profil statt Kreis: Hauptwurzeln 3.2×1.3cm (Aspect 2.5:1)
- Nebenwurzeln 1.2×0.6cm (Aspect 2:1)
- Fillet komplett entfernt (manuell, ~40 Zeilen weniger)
- Gleiche Struktur: 4 Haupt + 5 Neben, variable Exponenten
- 299 Zeilen (vs 326 v2)

## Abgeschlossen: Phase 7 – Hilfe-Command + Cleanup (2026-03-16)
- Neuer Command: "hilfe" / "help" → zeigt alle verfügbaren Commands
- Keywords: "was kannst du", "welche befehle", "was geht"
- HELP_TEXT Konstante in remote_commands.py: einzige Stelle für Command-Dokumentation
- CLAUDE.md: Regel ergänzt – neue Commands müssen in HELP_TEXT nachgetragen werden
- "selfie" als Keyword ergänzt (Satz-Erkennung: "schick mir bitte ein selfie")
- demo_matrix_bot.py: AvatarRenderer, SecretStore, project_root in RemoteCommandHandler verdrahtet
- Alte Platzhalter-Sprites gelöscht (saleria-*.png, Dead-Code seit LayeredSpriteRenderer)
- Tests: 701/701 grün

## Phase 7 – Remote-Features: ABGESCHLOSSEN
- Alle Schritte erledigt (Tier 1-3 + ClaudeAgent + Alerts + Avatar + Hilfe)
- Neue Klassen: RemoteCommandHandler, ClaudeAgent, AlertMonitor, AlertConfig
- Commands: 20+ (status, screenshot, media, volume, clipboard, send_file, start/kill,
  wol, git, docker, download, avatar/selfie, hilfe, claude)
- Sicherheit: Whitelists (Start/Kill/Git/Docker), Größenlimits, Pfad-Validierung
- ABC-Erweiterungen: MessageChannel.send_file(), AvatarRenderer.render_to_file()
- Konsistentes Pattern: DI + Graceful Degradation (wie alle Module)
- 701 Tests, alle grün

## Abgeschlossen: Phase 7 – Restart-Command (2026-03-16)
- Branch: feature/phase-7-tier2-tier3-alerts
- Neuer Command: "restart" / "neustart" → Bot startet sich selbst neu
- Keywords: "starte neu", "bitte neustarten"
- Flow: Nachricht senden → Restart-Flag schreiben (room_id) → os.execv() (Prozess ersetzt sich)
- Startup: Flag-Datei prüfen → "Bin wieder da!" an Matrix senden → Flag löschen
- Use-Case: git pull → restart → neuer Code aktiv, ohne manuellen Eingriff
- Datei: src/elder_berry/comms/remote_commands.py (erweitert: restart Command + CommandResult.restart Flag)
- Datei: src/elder_berry/comms/bridge.py (erweitert: _perform_restart + send_restart_notification)
- HELP_TEXT aktualisiert
- Tests: 720/720 grün (12 neu + 708 bestehend)
- Alte Platzhalter-Sprites gelöscht (saleria-*.png)

## In Arbeit: Phase 5 – TS0 LLMRouter (AnthropicClient) + TS1 RAG MemoryStore
- Branch: feature/phase-5-memory
- TS0: AnthropicClient (Sonnet 4.6 als primäres Backend), LLMRouter primary/fallback refactor
- TS1: MemoryStore ABC + ChromaMemoryStore (ChromaDB + OllamaEmbeddingClient)
- Nächste Dateien: llm/anthropic_client.py, llm/router.py, memory/base.py, memory/chroma_memory.py

## Zwischenstand: Phase 5 – TS0 abgeschlossen (2026-03-16)
- AnthropicClient: Sonnet 4.6, lazy-Import, skipif-Tests für Devcontainer
- LLMRouter: primary/fallback Pattern, create_default() → Anthropic → Ollama
- OllamaClient + OpenRouterClient: name-Attribut ergänzt
- pyproject.toml: anthropic in core deps, [memory] group (chromadb) vorbereitet
- .env.example: neu strukturiert (Anthropic primär, Ollama Fallback, Memory)
- Tests: 654 passed, 13 skipped (4 neue skips: anthropic-Paket fehlt im Devcontainer)
- Nächster Schritt: TS1 – MemoryStore ABC + ChromaMemoryStore

## Abgeschlossen: Phase 5 – TS0 + TS1 – LLMRouter + RAG MemoryStore (2026-03-16)
- Branch: feature/phase-5-memory

### TS0 – LLMRouter: Anthropic (Sonnet 4.6) als primäres Backend
- Neue Datei: src/elder_berry/llm/anthropic_client.py (AnthropicClient)
  - Sonnet 4.6 Default, Lazy-Import, name="anthropic"
  - is_available(): ANTHROPIC_API_KEY gesetzt?
  - generate(): system-Parameter als Anthropic-System-Feld
  - Robuste Fehlerbehandlung: APIStatusError, APIConnectionError, RateLimitError
- Geändert: src/elder_berry/llm/router.py (primary/fallback statt ollama/openrouter)
  - create_default(): AnthropicClient → OllamaClient
  - active_backend: nutzt client.name Attribut
- Geändert: src/elder_berry/llm/ollama_client.py + openrouter_client.py (name-Attribut)
- Geändert: pyproject.toml (anthropic in core deps, [memory] group: chromadb)
- Geändert: .env.example (neu strukturiert, ANTHROPIC_API_KEY primär)
- Geändert: tests/test_llm_router.py (30 Tests, 4 skips: anthropic-Paket fehlt)

### TS1 – RAG MemoryStore: ChromaDB + Ollama-Embeddings
- Neue Datei: src/elder_berry/memory/base.py (MemoryStore ABC + MemoryEntry + MemoryContext)
  - MemoryEntry: frozen Dataclass, create() Factory mit UUID + UTC-Timestamp
  - MemoryContext: recent + relevant, to_prompt_text(), is_empty()
  - MemoryStore: add(), get_recent(), search(), get_context(), new_session(), clear()
- Neue Datei: src/elder_berry/memory/embedding.py (EmbeddingClient ABC + OllamaEmbeddingClient)
  - nomic-embed-text Default, 768-dim Vektoren, Text-Kürzung bei >8000 Zeichen
- Neue Datei: src/elder_berry/memory/chroma_memory.py (ChromaMemoryStore)
  - Persistent ChromaDB, Lazy-Init, pluggable EmbeddingFunction
  - _OllamaEmbeddingFunction: Wrapper für ChromaDB-Interface
  - get_recent(): Sortierung nach Timestamp in Python (Chromadb kennt kein ORDER BY)
  - search(): exclude_session-Filter für Cross-Session-Suche
- Geändert: src/elder_berry/core/assistant.py
  - memory: MemoryStore als optionaler DI-Parameter
  - session_id: auto-generiert bei Init
  - _get_memory_context(): ruft get_context() ab, graceful fallback
  - _save_to_memory(): speichert user + assistant Message nach process()
  - _build_system_prompt(): memory_context Parameter
  - new_session(): Session-Reset
- Geändert: src/elder_berry/character/base.py + saleria.py (memory_context Parameter)
- Geändert: src/elder_berry/character/saleria.yaml ({memory_context} Platzhalter)
- Neue Datei: tests/test_memory_store.py (35 Tests, 23 passed + 12 skipped chromadb)

### Gesamtergebnis
- Tests: 677 passed, 25 skipped (all green)
- 23 neue skips: 4 anthropic + 12 chromadb (Pakete fehlen im Devcontainer)
- Tower-Setup: pip install chromadb + ollama pull nomic-embed-text + ANTHROPIC_API_KEY

## In Arbeit: Phase 5 – TS2 Startup-Script + TS3 STT (Whisper)
- Branch: feature/phase-5-memory (weitergeführt)
- TS2: scripts/start_saleria.py – vollständiger Stack-Start (Terminal/Matrix/Voice-Modus)
- TS3: STTEngine ABC + FasterWhisperEngine (faster-whisper, GPU-beschleunigt)
- Nächste Dateien: scripts/start_saleria.py, stt/base.py, stt/faster_whisper_engine.py

## Abgeschlossen: Phase 5 – TS2 Startup-Script + TS3 STT (2026-03-16)
- Branch: feature/phase-5-memory

### TS2 – Startup-Script: scripts/start_saleria.py
- Drei Modi: terminal, matrix (Standard), voice
- Flags: --no-memory, --no-tts, --no-avatar, --whisper-model, --debug
- Initialisiert: LLMRouter, ActionsDB, ActionController (Windows/Dummy), TTSEngine,
  SaleriaEngine, MemoryStore, SystemMonitor, Avatar, STTEngine (nur voice-Modus)
- Matrix-Modus: MatrixBridge + RemoteCommandHandler + ClaudeAgent + AlertMonitor
- Graceful Degradation: fehlende Pakete/Windows-APIs → Fallback ohne Crash
- Platform-Erkennung: Windows → WindowsActionController, sonst → Dummy

### TS3 – STT: FasterWhisperEngine
- Neue Dateien: src/elder_berry/stt/base.py (STTEngine ABC + DTOs)
  - TranscriptionSegment: frozen Dataclass (start, end, text)
  - TranscriptionResult: text, language, confidence, segments
  - STTEngine: transcribe(Path), transcribe_bytes(bytes), load/unload, is_available
- Neue Datei: src/elder_berry/stt/faster_whisper_engine.py (FasterWhisperEngine)
  - Auto-Device: CUDA wenn verfügbar, sonst CPU
  - Auto-ComputeType: float16 (CUDA) / int8 (CPU)
  - VAD-Filter: reduziert Halluzinationen bei Stille
  - Lazy-Load + explizites unload() für VRAM-Management
  - transcribe_bytes(): PCM-Bytes → temp WAV → transcribe()
  - Konfidenz: avg_logprob → exp() → 0.0–1.0
- Tower-Empfehlung: large-v3 (beste Qualität, ~3GB VRAM)
- pyproject.toml: [stt] optional group (faster-whisper, sounddevice, numpy)
- Tests: 683 passed, 44 skipped (19 neue skips: faster-whisper fehlt im Devcontainer)

### Tower Setup (vollständig):
  pip install -e ".[windows,tts-neural,avatar,matrix,remote,memory,stt]"
  pip install chromadb faster-whisper
  ollama pull nomic-embed-text
  ANTHROPIC_API_KEY in .env
  python scripts/start_saleria.py --mode terminal   # Test
  python scripts/start_saleria.py                   # Matrix-Betrieb

## Abgeschlossen: Phase 5 – TS4 Matrix-STT (Sprachnachrichten → Text → Saleria) (2026-03-16)
- Branch: feature/phase-5-memory
- Geändert: src/elder_berry/comms/message_channel.py
  - IncomingMessage: neues Feld audio_data: bytes | None = None
- Geändert: src/elder_berry/comms/matrix_channel.py
  - Import: RoomMessageAudio, DownloadError, DownloadResponse
  - connect(): _on_room_audio als Callback für RoomMessageAudio registriert
  - _on_room_audio(): MXC-URL parsen → download() → IncomingMessage mit audio_data → Callbacks
  - Fehlerbehandlung: DownloadError, ungültige URL, Exception → kein Crash
- Geändert: src/elder_berry/comms/bridge.py
  - stt: STTEngine | None = None (neuer DI-Parameter)
  - _handle_message(): Audio-Routing VOR Command-Router (msg.audio_data is not None)
  - _handle_audio_message(): audio_data → temp .ogg → STT.transcribe() → text_msg → _handle_assistant_message()
    - Ohne STT: Hinweismeldung senden
    - STT leer: "nicht verstanden" Meldung senden
    - STT OK: Transkript als Text-Nachricht weiterverarbeiten (inkl. TTS-Antwort)
    - Exception: Fehlermeldung senden + error_log
- Geändert: scripts/start_saleria.py
  - init_stt(): jetzt auch für matrix-Modus (optional, kein sys.exit bei ImportError)
  - run_matrix(): stt-Parameter + MatrixBridge(stt=stt)
  - Bug Fix: remote_handler=remote → remote_commands=remote
- Tests:
  - tests/test_comms.py: 9 neue Tests (TestIncomingMessageAudio: 3, TestBridgeAudioRouting: 6)
  - tests/test_matrix_channel.py: 8 neue Tests (TestOnRoomAudio: 8, DownloadError/DownloadResponse Import)
- Tests: 700 passed, 44 skipped (alle grün)

### Flow (live):
  Element: 🎤 Sprachnachricht
      ↓
  MatrixChannel._on_room_audio(): MXC → download() → IncomingMessage(audio_data=bytes)
      ↓
  MatrixBridge._handle_audio_message(): temp .ogg → FasterWhisperEngine.transcribe()
      ↓
  text_msg → _handle_assistant_message() → Saleria.process() → TTS → OGG → Matrix

## Abgeschlossen: TTS-Fix – XTTS Sprach-Drift bei kurzen Texten (2026-03-16)
- Problem: "Guten Morgen, wie kann ich dir helfen" → XTTS splittet intern am Komma
- "Guten Morgen" (12 Zeichen) zu kurz → Modell driftet ins Japanische
- Fix: split_sentences=False für kurze Texte (<100 Zeichen) in CoquiTTSEngine
- Datei: src/elder_berry/tts/coqui_engine.py (generate_audio: split-Logik)
- Tests: 3 neue Tests (TestSplitSentences), 44/44 grün
- Zum Testen: "Guten Morgen, wie kann ich dir helfen?" sollte jetzt komplett deutsch bleiben

## Abgeschlossen: Live-Test-Fixes + TTS Concat (2026-03-16)
- Branch: feature/phase-7-tier2-tier3-alerts
- Erster vollständiger Live-Test aller Komponenten über Matrix

### start_saleria.py – 6 Fixes
- load_secrets_to_env(): SecretStore-Keys → os.environ (LLMRouter findet Anthropic)
- init_tts(character=character): Character VOR TTS erstellt, Voice-Map übergeben
- RemoteCommandHandler: alle DI-Params (secret_store, project_root, avatar_renderer)
- init_audio_converter(): WAV→OGG für Matrix-Sprachantworten
- error_log_dir + alert_room_id an MatrixBridge übergeben
- Parameterfehler: action_controller → controller

### Matrix-Audio-Download
- Synapse 1.149.1 blockiert unauthentifizierte Media-Downloads
- Fix: _authenticated_download() in MatrixChannel (Bearer-Token + Fallback auf nio)
- Endpoint: /_matrix/client/v1/media/download (statt /_matrix/media/v3/download)

### ChromaDB-Kompatibilität
- get_or_create_collection(): get_or_create=True Keyword entfernt (redundant)
- _OllamaEmbeddingFunction: name-Attribut ergänzt (neuere ChromaDB erfordert es)

### TTS Sprach-Drift Fix v2 (WAV-Concatenation)
- Problem: auch bei 120+ Zeichen driftet XTTS bei Coqui-internem Splitting
- Lösung: IMMER split_sentences=False, eigenes Satz-Splitting + WAV-Concat
- _split_sentences(): Split an .!?…, kurze Fragmente (<15 Zeichen) → an vorherigen Satz
- _generate_and_concat(): pro Satz einzeln generieren (voller Speaker-Kontext) → wave concat
- Einzelne Sätze: direkt generieren, kein Overhead

### STT → Command-Router
- Fix: STT-Transkription geht jetzt durch _handle_message() statt _handle_assistant_message()
- Effekt: Sprachnachricht "Screenshot" wird korrekt als Command erkannt

### Ergebnis
- Anthropic als primäres LLM-Backend (Sonnet 4.6)
- TTS mit Voice-Map (10 Emotionen) funktional
- Sprachnachrichten: Element → Whisper STT → Command/LLM → TTS → OGG → Matrix
- Tests: 815/815 grün

## Abgeschlossen: Phase 8 – TS1 Google Calendar + TS2 Email (IMAP) (2026-03-16)
- Branch: feature/phase-8-calendar-email

### TS1 – Google Calendar
- Neue Datei: src/elder_berry/tools/google_calendar.py (GoogleCalendarClient + CalendarEvent DTO)
  - OAuth2-Tokens aus SecretStore (Refresh-Token, auto-refresh)
  - get_today(), get_tomorrow(), get_events(days=N), create_event()
  - format_events(): Formatierung mit Datum-Gruppierung
  - CalendarEvent: summary, start, end, location, all_day, format_short()
  - Lazy-Init: Google API Service erst bei erstem Zugriff
- Neue Datei: scripts/setup_google_oauth.py (Einmaliges OAuth2-Setup)
  - Browser-Flow → Refresh-Token → SecretStore (verschlüsselt)
  - Scopes: calendar (lesen + schreiben)
- Commands: termine, termine morgen, termine woche, termin: Titel Datum Zeit
- Keywords: "was steht an", "welche termine", "kalender", "nächster termin"

### TS2 – Email (IMAP)
- Neue Datei: src/elder_berry/tools/email_client.py (IMAPEmailClient + EmailMessage DTO)
  - IMAP-Verbindung pro Aufruf (kein Langzeit-Socket)
  - get_unread(), get_recent(days=N), get_unread_count()
  - format_mails(): Kurzliste, format_mails_detailed(): für LLM-Zusammenfassung
  - _decode_header(): MIME-encoded Header (UTF-8, ISO-8859-1)
  - _extract_body(): text/plain bevorzugt, Fallback HTML (Tags entfernt)
  - from_secret_store(): email_imap_host, email_user, email_password
  - Keine extra Dependencies (Python-Standardbibliothek: imaplib, email)
  - Provider-unabhängig: Strato, GMX, Gmail, Outlook, etc.
- Commands: mails, mails N (letzte N Tage), mail zusammenfassung
- Keywords: "neue mails", "ungelesene mails", "posteingang"

### Integration
- RemoteCommandHandler: calendar + email_client als neue DI-Parameter
- HELP_TEXT: Kalender + E-Mail Sections ergänzt
- KEYWORD_MAP: termine, mails, mail_summary Keywords
- Regex: TERMINE_PATTERN, TERMIN_CREATE_PATTERN, MAILS_DAYS_PATTERN
- pyproject.toml: [tools] optional group (google-api-python-client, google-auth-oauthlib)
- Neue Tests: tests/test_google_calendar.py (15), tests/test_email_client.py (20),
  tests/test_remote_commands.py (20 neue: Calendar + Email Commands)
- Tests: 870/870 grün (55 neu + 815 bestehend)

### Setup für Live-Betrieb
- Calendar: python scripts/setup_google_oauth.py (einmalig)
- Email: SecretStore().set("email_imap_host", "imap.strato.de") etc.
- start_saleria.py: calendar + email_client + gym_client in RemoteCommandHandler verdrahtet

## Abgeschlossen: Phase 8 – TS3 Berry-Gym Integration (2026-03-16)
- Branch: feature/phase-8-calendar-email
- Neue Datei: src/elder_berry/tools/gym_data.py (GymDataClient)
  - Berry-Gym REST-API Client (Token-Auth, httpx, lazy-init)
  - get_summary(), get_last_training(), get_week(), get_prs()
  - format_summary/last_training/week/prs() – lesbare Text-Ausgabe
  - Base-URL konfigurierbar, Timeout 10s, Graceful Degradation
- Commands: training, training details, training woche, prs
- Keywords: "letztes training", "wie war mein training", "gym", "personal records"
- start_saleria.py: GymDataClient initialisiert wenn berry_gym_api_token im SecretStore
- Neue Tests: tests/test_gym_data.py (28 Tests)
- Setup: SecretStore().set("berry_gym_api_token", "<token>")

## Abgeschlossen: Phase 8 – Mail-Suche + Termin-Suche (2026-03-16)
- IMAPEmailClient.search(): IMAP OR-Suche (SUBJECT + FROM), serverseitig
- GoogleCalendarClient.search_events(): Google Calendar q-Parameter (Volltextsuche)
- Natürliche Sprache: "suche mir bitte die mail mit der Rechnung von Alux" funktioniert
- Regex: MAIL_SEARCH_PATTERN + TERMIN_SEARCH_PATTERN (zwei alternative Gruppen)
- Tests: 907/907 grün

## Abgeschlossen: Phase 8 – LLM-Fallback + Kontext-Memory (2026-03-16)
- LLM als Fallback-Router: Saleria kennt ihre Remote-Tools im System-Prompt
  - Wenn Pattern nicht matcht → LLM erkennt remote_command → Bridge führt Command aus
  - Doppelte Absicherung: Pattern spart API-Kosten, LLM fängt Rest auf
  - remote_command Aktion in saleria.yaml + SYSTEM_PROMPT_TEMPLATE
  - AssistantResult: neues Feld action_params (dict)
  - Bridge: _handle_llm_remote_command() extrahiert Command aus LLM-Params
- Kontext-Speicher (Quick-Fix): Bridge merkt sich letztes Command-Ergebnis pro User
  - "suche mail von X" → Ergebnis → "fasse die mail zusammen" → LLM bekommt Kontext
  - _last_command_result: dict[sender, str]
  - Kontext wird an user_input angehängt bei LLM-Aufrufen
- Termin-Keywords erweitert: "nächste woche", "diese woche", "habe ich morgen"
  - termine_woche / termine_morgen als separate Keyword-Varianten
  - TERMINE_PATTERN: nächste woche / diese woche ergänzt
- allowed_rooms in start_saleria.py gesetzt (keine Phantom-Join-Versuche mehr)
- Mail-Pattern erweitert: "Suche das Angebot in meinen Mails von X" matcht jetzt

### OFFEN: Echtes Konversations-Gedächtnis (Multi-Turn)
- Aktuell: nur letztes Command-Ergebnis als Kontext, kein Chat-Verlauf
- Richtige Lösung: letzte N Nachrichten pro User in der Bridge als Kontext-Fenster
  - Sliding Window (z.B. letzte 5 Messages) → an LLM als Chat-History übergeben
  - Getrennt von RAG-Memory (ChromaDB = Langzeit, Chat-History = Kurzzeit)
  - Ermöglicht: Rückfragen, "mach das nochmal", "und was ist mit..."
  - Aufwand: ~50-80 Zeilen in Bridge + Assistant

## Abgeschlossen: Gehäuse – RootPaths.iLogicVb v3 (L-Kurve + Ellipse)
- 9/9 Splines, Sweeps: noch zu bestätigen (v3 Fix für AddSimple angewendet)
- L-förmige Z-Kurve funktioniert: Drop→Flat→Dive, keine Sichelform mehr
- Elliptische Profile: breite, flache Rippen statt runde Finger
- Visuelles Ergebnis: erkennbar Baumstumpf-Wurzeln (Referenzbild-nah)
- Fillets entfernt (manuell setzen)
- Nächster Schritt: Feintuning der Wurzeln + manuelles Finishing in Inventor
- Hardware-Entscheidung: Drehteller = 28BYJ-48 Schrittmotor + Hall-Sensor (Ø4×2mm Neodym) für Homing

## In Arbeit: Gehäuse – RootFingers.iLogicVb (Finger-Wurzeln als separate Bodies)
- Datei: hardware/enclosure/iLogic/RootFingers.iLogicVb (NEU)
- Kleine krallenartige Wurzeln auf/neben den flachen Hauptwurzeln
- Kreisprofil, kNewBodyOperation → eigene Körper für Boolean-Verbindung

## Abgeschlossen: Gehäuse – RootFingers.iLogicVb (Finger-Wurzeln)
- Datei: hardware/enclosure/iLogic/RootFingers.iLogicVb (257 Zeilen, NEU)
- Zwei Gruppen: 6 am Stammfuß (Ø0.7cm, 3.5cm lang) + 8 auf Hauptwurzeln (Ø0.5cm, 2.5cm)
- Kreisprofil (nicht Ellipse) – krallenartig gewollt
- kNewBodyOperation → alle Finger als separate Bodies (für Boolean Combine)
- Sichelform-Kurve (gewollt bei kleinen Fingern, anders als Hauptwurzeln)
- Variable Exponenten (radialExp, absenkExp) pro Finger
- Taper-Sweep mit Fallback auf konstant
- Cleanup über Prefix RootFinger_S_ / RootFinger_W_
- Seed 77 (≠ BarkPaths 42, ≠ RootPaths 99)

## Abgeschlossen: Multi-Turn Chat-History + Mail-Erweiterungen (2026-03-17)
- Branch: feature/phase-9-chat-history-mail

### Multi-Turn Chat-History (Sliding Window)
- Neue Datei: src/elder_berry/comms/chat_history.py (ChatHistory + ChatMessage)
  - Sliding Window pro User (default 10 Nachrichten, FIFO)
  - format_for_prompt(): formatierter Text für LLM-Kontext
  - Getrennt von RAG-Memory (ChromaDB = Langzeit, ChatHistory = Kurzzeit)
- Geändert: src/elder_berry/comms/bridge.py
  - _last_command_result (Quick-Fix) → _chat_history: ChatHistory ersetzt
  - User-Nachrichten, Command-Ergebnisse und LLM-Antworten in History
  - Chat-History als Kontext an assistant.process() übergeben
  - file_paths-Support für mehrere Dateien (z.B. Mail-Anhänge)
- Geändert: src/elder_berry/core/assistant.py
  - process(): neuer Parameter chat_history: str = ""
  - _build_system_prompt(): chat_history ans Ende angehängt
  - Funktioniert mit CharacterEngine (saleria.yaml) und Fallback-Template
- Ermöglicht: "fasse die mail zusammen", "schick mir den anhang", Rückfragen

### Mail-Anhänge senden
- Geändert: src/elder_berry/tools/email_client.py
  - EmailMessage: neues Feld msg_id: str = "" (IMAP UID)
  - _fetch_mails(): IMAP UIDs statt Sequenznummern (stabiler)
  - format_short(): zeigt [#msg_id] im Output
  - format_mails_detailed(): zeigt (ID: msg_id) im Header
  - Neue Methode: get_attachments(msg_id) → list[(filename, bytes)]
    - IMAP UID-basierter Fetch, MIME-Walk, Attachment-Extraktion
- Geändert: src/elder_berry/comms/remote_commands.py
  - Neuer Command: mail anhang <ID> → Anhänge als Dateien senden
  - MAIL_ATTACHMENT_PATTERN: "mail anhang 42", "anhang von mail 42", "mail 42 anhang"
  - CommandResult: neues Feld file_paths: list[Path] (mehrere Dateien)
  - HELP_TEXT aktualisiert
- Geändert: src/elder_berry/character/saleria.yaml + assistant.py (System-Prompt)
  - "mail anhang <id>" in Remote-Tool-Liste ergänzt

### Tests
- Neue Datei: tests/test_chat_history.py (19 Tests)
  - ChatMessage DTO, add/get, sliding window, multi-user, clear, format
- Erweitert: tests/test_email_client.py (+11 Tests)
  - msg_id DTO, parse_email mit msg_id, get_attachments (4 Szenarien), format_detailed
- Erweitert: tests/test_remote_commands.py (+8 Tests)
  - mail_attachment: parse (4 Varianten), execute (no client, no attachments, with, error)
- Erweitert: tests/test_comms.py (+5 Tests)
  - Bridge Chat-History: passed to assistant, command result stored, response stored,
    multi-turn grows, file_paths sent
- Erweitert: tests/test_assistant.py (+2 Tests)
  - chat_history in system prompt, empty chat_history no extra text
- Tests: 825 passed, 36 skipped (alle grün, +55 neue Tests)
- Vorbestehende Skips: cryptography, pydub, chromadb, anthropic, faster-whisper fehlen

### Nachbesserungen (gleicher Chat)
- Mail-Suche: Kurzliste für User, Detail-Body für Chat-History (history_text Feld)
  - CommandResult: neues Feld history_text (alternativer Text für Chat-History)
  - Bridge: history_text bevorzugen wenn gesetzt
  - Effekt: "fasse die mail zusammen" funktioniert, weil LLM den Body im Kontext hat
- Termin-Erstellung: natürliche Datumsformate
  - TERMIN_CREATE_PATTERN erweitert: morgen, übermorgen, DD.MM, DD.MM.YYYY, "um HH:MM Uhr"
  - "erstelle termin ...", "erstelle einen termin ..." wird erkannt
  - _parse_natural_date(): Helfer-Funktion für Datums-Parsing
  - System-Prompt (saleria.yaml + assistant.py): "termin erstellen" in Tool-Liste
  - Tests: +10 neue Tests (parse + execute mit morgen/DD.MM/erstelle)
- Termin löschen: natürliche Sprache
  - CalendarEvent: neues Feld event_id: str = "" (Google Calendar Event-ID)
  - GoogleCalendarClient: delete_event(event_id) Methode
  - format_short(): zeigt [#event_id] im Output
  - _last_events: Handler merkt sich letzte Termin-Ergebnisse
  - Neue Commands: "lösche termin X", "lösche den 2. termin", "lösche alle termine"
  - TERMIN_DELETE_PATTERN: "termin löschen", "lösche termin", "entferne termin"
  - Index-Parsing: "1.", "den ersten", "den 2.", Ordinalzahlen 1-5
  - Titel-Suche: "lösche termin Zahnarzt" (in _last_events)
  - Fallback: direkte Event-ID, Fehler bei Mehrdeutigkeit
  - System-Prompt (saleria.yaml + assistant.py): "termin löschen" in Tool-Liste
  - HELP_TEXT aktualisiert
  - Tests: +19 neue Tests (CalendarEvent event_id, parse, delete by index/title/alle/error)
- Tests gesamt: 844 passed, 36 skipped

## Roadmap-Änderung: Intent-Classifier gestrichen (2026-03-17)
- Phase "Intent-Classifier (Self-Learning Command-Routing)" aus PROJECT_ROADMAP.md entfernt
- Begründung: Problem existiert bei aktuellem Scope nicht – Regex+Keywords+LLM-Fallback reicht
  - Embedding-Similarity unterscheidet nicht zwischen Konversation und Command (semantisch zu ähnlich)
  - Parameter-Extraktion (Datum, Index, Suchbegriff) bleibt ohnehin bei Regex
  - Self-Learning ohne Feedback-Loop birgt Drift-Risiko
  - Kann später reaktiviert werden wenn Command-Anzahl oder Fehlerkennungen das rechtfertigen
- Phasen umbenannt: Phase 10 → 9 (Multimodal+Autonomie), Phase 11 → 10 (Hardware-Abschluss)

## In Arbeit: Phase 8.1–8.3 – Wetter, Timer & Erinnerungen, Daily Briefing
- Branch: feature/phase-8-1-weather-timer-briefing
- Konzept: docs/concepts/phase-8-1-8-3-weather-timer-briefing.md
- Nächster Schritt: 8.1 – WeatherClient (Open-Meteo API, kein Key nötig)

## Zwischenstand: Phase 8.1 – WeatherClient abgeschlossen (2026-03-17)
- Neue Datei: src/elder_berry/tools/weather_client.py (WeatherClient + DTOs + WMO-Mapping)
  - Open-Meteo API (kostenlos, kein Key), Standort aus SecretStore
  - get_current(), get_today(), get_days(N), format_current(), format_forecast()
  - WeatherData + WeatherForecast DTOs (frozen), WMO_DESCRIPTIONS + WMO_EMOJIS
- Neue Datei: tests/test_weather_client.py (22 Tests)
- Geändert: remote_commands.py (WEATHER_PATTERN, KEYWORD_MAP, HELP_TEXT, _cmd_weather)
- Geändert: saleria.yaml + assistant.py (Remote-Tool-Liste: wetter)
- Geändert: start_saleria.py (WeatherClient init + DI)
- Tests: 220/220 grün (test_weather_client + test_remote_commands)
- Nächster Schritt: 8.2 – Timer & Erinnerungen

## Zwischenstand: Phase 8.2 – Timer & Erinnerungen abgeschlossen (2026-03-17)
- Neue Datei: src/elder_berry/tools/reminder_store.py (ReminderStore + Reminder DTO + SQLite)
  - Pfad: ~/.elder-berry/reminders.db, Thread-safe, WAL-Modus
  - add(), get_pending(), get_due(), mark_fired(), cancel(), cancel_all(), format_pending()
  - Alle Zeiten intern UTC, Anzeige in Lokalzeit
- Neue Datei: src/elder_berry/comms/reminder_scheduler.py (ReminderScheduler)
  - Daemon-Thread, 15s Poll-Intervall, Callback für Matrix
  - Retry bei Callback-Fehler (Erinnerung bleibt unfired)
- Neue Tests: test_reminder_store.py (17), test_reminder_scheduler.py (6)
- Geändert: remote_commands.py (TIMER/REMINDER/REMINDER_DELETE_PATTERN, KEYWORD_MAP, HELP_TEXT)
  - _cmd_timer(), _cmd_reminder(), _cmd_erinnerungen(), _cmd_reminder_delete()
  - _parse_duration(): min/stunde/sek → timedelta
  - REMINDER_DELETE vor TERMIN_DELETE (Parse-Reihenfolge Fix)
- Geändert: bridge.py (ReminderScheduler DI + start/stop, AlertMonitor-Pattern)
- Geändert: saleria.yaml + assistant.py (Timer/Erinnerungen in Tool-Liste)
- Geändert: start_saleria.py (ReminderStore + ReminderScheduler init)
- Tests: 44/44 grün (Weather + Timer + TerminDelete)
- Nächster Schritt: 8.3 – Daily Briefing

## Abgeschlossen: Phase 8.1–8.3 – Wetter, Timer & Erinnerungen, Daily Briefing (2026-03-17)
- Branch: feature/phase-8-1-weather-timer-briefing

### 8.1 – WeatherClient (Open-Meteo)
- Neue Datei: src/elder_berry/tools/weather_client.py (WeatherClient + DTOs + WMO-Mapping)
  - Open-Meteo API (kostenlos, kein Key), Standort aus SecretStore
  - get_current(), get_today(), get_days(N), format_current(), format_forecast()
  - WeatherData + WeatherForecast DTOs (frozen), WMO_DESCRIPTIONS + WMO_EMOJIS
  - Lazy-Init httpx.Client, Emoji-Mapping, Tagesnamen (Heute/Morgen/Di 17.03.)
- Commands: wetter, wetter morgen, wetter woche, wetter N
- Keywords: "regnet es", "brauche ich einen schirm", "wie warm", "wettervorhersage"

### 8.2 – Timer & Erinnerungen
- Neue Datei: src/elder_berry/tools/reminder_store.py (ReminderStore + Reminder DTO + SQLite)
  - Pfad: ~/.elder-berry/reminders.db, Thread-safe, WAL-Modus
  - add(), get_pending(), get_due(), mark_fired(), cancel(), cancel_all(), format_pending()
  - Alle Zeiten intern UTC, Anzeige in Lokalzeit, Naive-Check (ValueError)
- Neue Datei: src/elder_berry/comms/reminder_scheduler.py (ReminderScheduler)
  - Daemon-Thread, 15s Poll, Retry bei Callback-Fehler (Erinnerung bleibt unfired)
- Commands: timer N min/stunde/sek, erinnere mich um/in, erinnerungen, lösche erinnerung
- _parse_duration(): min/stunde/sek → timedelta
- Fix: REMINDER_DELETE vor TERMIN_DELETE in Parse-Reihenfolge

### 8.3 – Daily Briefing
- Neue Datei: src/elder_berry/comms/briefing_scheduler.py (BriefingScheduler)
  - Daemon-Thread, 30s Poll, einmal täglich zur konfigurierten Uhrzeit (Default 07:30)
  - build_briefing(): Wetter + Kalender + Erinnerungen kombiniert
  - Graceful Degradation: fehlende Services → Abschnitt weggelassen
  - Kein Briefing wenn keine Daten (leerer String)
- Command: briefing (manuell), Keywords: "guten morgen", "was steht heute an"

### Integration
- Geändert: src/elder_berry/comms/remote_commands.py
  - +weather, +reminder_store, +briefing_scheduler DI-Parameter
  - +WEATHER_PATTERN, +TIMER_PATTERN, +REMINDER_PATTERN, +REMINDER_DELETE_PATTERN
  - +KEYWORD_MAP: wetter, erinnerungen, briefing
  - +HELP_TEXT: Wetter, Timer & Erinnerungen, Briefing Sections
  - +_cmd_weather(), +_cmd_timer(), +_cmd_reminder(), +_cmd_erinnerungen(), +_cmd_reminder_delete(), +_cmd_briefing()
- Geändert: src/elder_berry/comms/bridge.py
  - +reminder_scheduler, +briefing_scheduler DI-Parameter
  - +_start_reminder_scheduler(), +_start_briefing_scheduler() (AlertMonitor-Pattern)
  - Start/Stop in _async_main(), stop(), _perform_restart()
- Geändert: src/elder_berry/character/saleria.yaml + core/assistant.py (Remote-Tool-Liste)
- Geändert: scripts/start_saleria.py (WeatherClient, ReminderStore, ReminderScheduler, BriefingScheduler)

### Tests
- Neue Dateien:
  - tests/test_weather_client.py (22 Tests)
  - tests/test_reminder_store.py (17 Tests)
  - tests/test_reminder_scheduler.py (6 Tests)
  - tests/test_briefing_scheduler.py (10 Tests)
- Erweitert: tests/test_remote_commands.py (+30 Tests: Weather, Timer, Briefing)
- Tests: 934 passed, 36 skipped (alle grün, vorbestehende Skips: cryptography, pydub, chromadb, anthropic, faster-whisper)

### Setup für Live-Betrieb
- Wetter: SecretStore().set("weather_latitude", "52.52") etc.
- Timer/Erinnerungen: funktioniert sofort (SQLite, kein Setup)
- Briefing: automatisch wenn Wetter/Kalender/Erinnerungen konfiguriert

## Abgeschlossen: Bugfix – Mail per ID abrufen ("fasse mail #99 zusammen") (2026-03-17)
- Bug: "fasse mail #99 zusammen" wurde als MAIL_SEARCH_PATTERN gefangen → suchte "#99" statt Mail 99 zu holen
- Neue Methode: IMAPEmailClient.get_by_uid(msg_id) → EmailMessage | None
- Neues Pattern: MAIL_ID_PATTERN ("mail 99", "mail #99", "fasse mail #99 zusammen", "zeig mail 42")
- Neuer Handler: _cmd_mail_by_id() → text (Kurzinfo) + history_text (Body für LLM-Kontext)
- Parse-Reihenfolge: MAIL_ID_PATTERN VOR MAIL_ATTACHMENT_PATTERN und MAIL_SEARCH_PATTERN
- HELP_TEXT aktualisiert
- Tests: +12 neue Tests (3 get_by_uid + 6 parse + 3 execute)

## In Arbeit: Phase 10 – RPi5 Avatar-Display (Saleria zieht ein)
- Branch: feature/phase-10-rpi5-avatar-display
- Hardware: Raspberry Pi Touch Display 2 (5", 720×1280, DSI, Portrait, kapazitiv)
- Nächster Schritt: Deployment auf RPi5 + Live-Test

### RPi5AvatarDisplay
- Neue Datei: src/elder_berry/robot/rpi5_avatar.py
  - Klasse RPi5AvatarDisplay(AvatarDisplay) – echte Implementierung der ABC
  - Wraps LayeredSpriteRenderer, Render-Loop in Background-Thread (30 FPS)
  - Thread-safe: Emotion + Speaking über Lock gesetzt
  - start() / stop() für Lifecycle-Management
  - Default: 720×1280 Fullscreen (DSI-Display)

### LayeredSpriteRenderer – Fullscreen-Support
- Geändert: src/elder_berry/avatar/layered_renderer.py
  - initialize(): neuer Parameter fullscreen: bool = False
  - Fullscreen: pygame.FULLSCREEN | pygame.NOFRAME, Maus versteckt
  - Sprites werden automatisch auf 720×1280 skaliert (smoothscale)
- Geändert: src/elder_berry/avatar/base.py (ABC-Signatur: fullscreen Parameter)
- Geändert: src/elder_berry/avatar/sprite_renderer.py (Signatur-Kompatibilität)

### RPi5 Startup-Script
- Neue Datei: scripts/start_rpi5.py
  - RPi5AvatarDisplay + RobotServer (FastAPI, Port 8000)
  - --windowed / --width / --height / --port CLI-Argumente
  - Graceful Shutdown (SIGINT, SIGTERM)
  - Motoren + Sensoren: erstmal Simulator (echte Hardware eigene Phase)

### Tests
- Neue Datei: tests/test_rpi5_avatar.py (18 Tests)
  - Init + State (4), set_emotion/speaking (5 inkl. Concurrent),
    Render-Loop (4), Fullscreen (2), LayeredRenderer Fullscreen (3)
- Bestehende Tests: 23/23 avatar_renderer.py weiterhin grün

### Bugfix: Lip-Sync Reset (2026-03-17)
- Bug: RPi5AvatarDisplay rief jeden Frame show_speaking(True) → lip_sync_index wurde 30x/s auf 0 zurückgesetzt → Mund blieb offen
- Fix: show_speaking() ignoriert Aufrufe ohne Zustandswechsel (True→True = noop)
- Tests: +2 neue Tests (TestLipSyncFix)

### Feature: Idle-Animationen (2026-03-17)
- Saleria macht im Idle zufällige Bewegungen (alle 5-15 Sekunden, 2s Dauer):
  - glance_left/right: schaut zur Seite
  - smile: kurzes Lächeln
  - soft_close: Augen kurz schließen (nachdenklich)
  - surprise: überrascht schauen
- Deaktiviert während Sprechen (Lip-Sync hat Priorität)
- Prioritäts-Kette: Blink > Idle > Emotion-Default
- Tests: +4 neue Tests (TestIdleAnimations)

### Tower ↔ RPi5 Verdrahtung (2026-03-17)
- start_saleria.py: RobotClient wird initialisiert wenn "robot_host" im SecretStore
- SecretStore Key: robot_host = "http://192.168.50.220:8000"
- Flow: Matrix-Nachricht → LLM → Emotion → RobotClient → RPi5 → Avatar wechselt
- RPi5 Setup-Anleitung in README.md ergänzt (systemd-Autostart)

## Hardware-Meilenstein: RPi5 Display + Avatar live (2026-03-17)

### Display am RPi5 in Betrieb
- RPi Touch Display 2 (5", 720×1280, Portrait) angeschlossen und funktional
- Problem: pygame (SDL 2.28.4) auf Bookworm Lite hatte keinen Display-Driver
  - Alle Driver fehlten: kmsdrm, fbcon, directfb, egl, wayland, x11 (nur dummy)
  - System-SDL2 war ohne Display-Backends kompiliert (normal für Lite)
- Lösung: `pip install pygame-ce` (Community Edition, bringt eigene SDL2 mit kmsdrm)
  - Drop-in Replacement, gleiche API, kein Code-Änderung
  - Start: `SDL_VIDEODRIVER=kmsdrm python scripts/start_rpi5.py`
- Saleria rendert auf physischem Display: 720×1280 fullscreen, 21 Komponenten, 30 FPS
- Steuerung über REST API funktioniert: POST /avatar {"emotion": "cheerful"} etc.

### Gehäuse-Design (Inventor CAD)
- Innenraum-Anordnung definiert: RPi5 zentral unten, Display horizontal darüber
- Pepper's Ghost: Display leuchtet nach oben → 45° Acrylscheibe → Betrachtungsfenster seitlich
- Display-Halterung: Nut zum Einlegen + 6 Niederhalter (3 pro Seite), schraubenfrei
- ULN2003 Treiberboard neben RPi5 platziert (kurze Kabelwege zum Stepper)
- Belüftung: Luftspalte unten (zwischen Wurzeln) → Kamineffekt → Abluft durch Ast rechts
  - RPi5 hat ~5cm Freiraum ringsum, passive Konvektion reicht für ~10W
- Innenraum wird matt-schwarz lackiert (Hologramm-Effekt)

### Drehteller-Konzept finalisiert
- Motor: 28BYJ-48 Stepper (5V, ULN2003 Treiber, 4096 Steps/Umdrehung)
  - Kein Servo: volle 360° möglich, leise, langsam, präzise
- Drehwinkel: ±180° (begrenzt durch Stromkabel)
- USB-C Verlängerung (150cm F→M) nach unten durch Drehachse verlegt
  - Einziges Kabel das die Grenze feststehend↔drehend überquert
  - DSI/CSI/Sensoren sitzen alle auf dem Drehteller, drehen sich mit
- Endanschlag: beidseitige Magnete als harter mechanischer Stopp
  - Hall-Sensor (A3144) erkennt gleichzeitig Home-Position
- ULN2003 VCC: extern 5V (nicht RPi5 GPIO, wegen 240mA Strombedarf)

### Bestellungen
- Bestellt (BerryBase): Raspberry Pi Camera Module 3 (75°), GY-BME280, APDS-9960
- Angekommen: 28BYJ-48 Stepper + ULN2003, A3144 Hall-Sensoren (20 Stück)
- Vorhanden: Dupont-Kabel-Set (F-F/M-M/F-M 10cm)

### Entscheidungen
- Keine PCB: Verdrahtung mit Dupont-Steckern + gedruckte Kabelführung reicht
- Kein interner Akku: USB-C Netzteil für Dauerbetrieb, Anker Powerbank für unterwegs
- Kamera normal (75°) statt Wide (120°): Schreibtisch-Abstand ~60-80cm, 75° erfasst genug
- BerryBase statt Amazon: kleineren Laden unterstützen, Kamera dort auch günstiger

### Roadmap-Änderung
- Phase "Intent-Classifier (Self-Learning Command-Routing)" aus PROJECT_ROADMAP.md entfernt
  - Problem existiert bei aktuellem Scope nicht, Regex+Keywords+LLM-Fallback reicht
  - Phasen umbenannt: Phase 10→9, Phase 11→10
- Konzept-Dokument erstellt: docs/concepts/phase-8-1-8-3-weather-timer-briefing.md
  - Phase 8.1 Wetter (Open-Meteo), 8.2 Timer & Erinnerungen, 8.3 Daily Briefing

### Nächster Schritt
- Komponentenpositionierung im Gehäuse finalisieren (Kamera, Sensoren, Hall-Sensor)

---

## Abgeschlossen: Phase 11 – Dokument-Zusammenfassung (PDF/TXT via LLM) (2026-03-18)

### Neue Dateien
- src/elder_berry/tools/document_reader.py: DocumentReader Klasse
  - read_pdf() via pymupdf/fitz, read_txt() mit UTF-8/Latin-1 Fallback
  - read_file() Dispatcher, is_supported() Format-Check
  - DocumentResult DTO (frozen dataclass)
  - Max-Zeichen-Limit konfigurierbar (Default 100k), Truncation mit Hinweis
  - Graceful: lesbarer Fehler wenn pymupdf fehlt, Hinweis bei gescannten PDFs
- tests/test_document_reader.py: 26 Tests (DTO, is_supported, read_txt, read_pdf, Dispatcher, Truncation)

### Geänderte Dateien
- pyproject.toml: [documents] Dependency-Gruppe mit pymupdf>=1.23
- src/elder_berry/comms/remote_commands.py:
  - DOCUMENT_SUMMARY_PATTERN Regex ("zusammenfassung <pfad>", "fasse zusammen <pfad>")
  - _cmd_document_summary() Handler mit DocumentReader als DI-Dependency
  - HELP_TEXT: Dokumente-Sektion ergänzt
  - KEYWORD_MAP: "document_summary" Keywords
- src/elder_berry/comms/bridge.py:
  - DocumentReader als DI-Parameter
  - _handle_file_message(): Matrix m.file → temp → DocumentReader → LLM-Zusammenfassung
  - Extrahierter Text in Chat-History (Rückfragen möglich)
- src/elder_berry/comms/matrix_channel.py:
  - RoomMessageFile Import + _on_room_file() Callback
  - Download via _authenticated_download() (gleich wie Audio)
- src/elder_berry/comms/message_channel.py:
  - IncomingMessage: file_data + file_name Felder
- src/elder_berry/character/saleria.yaml: "zusammenfassung <pfad>" in Remote-Tool-Liste
- tests/test_remote_commands.py: +14 Tests (Pattern, ParseCommand, Execute)

### Tests
- 1147 Tests gesamt, alle grün (vorher 1101)
- Neue Tests: 26 (DocumentReader) + 14 (Remote Commands) = 40

### Zwei Wege zur Zusammenfassung
1. Remote Command: "zusammenfassung C:\...\datei.pdf" → DirectReader → Text an Matrix
2. Matrix File-Upload: PDF/TXT hochladen → Download → DocumentReader → LLM → Zusammenfassung
   - LLM-Zusammenfassung nur bei File-Upload (Kontext-reich)
   - Remote Command liefert den Rohtext (LLM entscheidet über Zusammenfassung via Fallback)

### Nächster Schritt
